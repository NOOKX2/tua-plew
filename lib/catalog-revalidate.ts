import { CATALOG_REVALIDATE_SECONDS } from "./catalog-cache";

/** ISR interval for public catalog pages (home, community, campaigns, shop, …). */
export const CATALOG_PAGE_REVALIDATE = CATALOG_REVALIDATE_SECONDS;
